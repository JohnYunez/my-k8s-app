apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-app-2048-yunez.fullname" . }}-test-connection"
  labels:
    {{- include "my-app-2048-yunez.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-app-2048-yunez.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
