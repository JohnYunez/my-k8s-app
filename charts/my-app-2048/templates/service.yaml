apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-app-2048.fullname" . }}
  labels:
    {{- include "my-app-2048.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.service.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-app-2048.selectorLabels" . | nindent 4 }}
